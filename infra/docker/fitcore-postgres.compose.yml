services:
  fitcore_postgres:
    image: postgres:17.6-alpine
    container_name: fitcore_postgres
    restart: unless-stopped
    env_file:
      - ${FITCORE_POSTGRES_ENV_FILE:-/opt/fitcore-pro/storage/secrets/fitcore-postgres.env}
    ports:
      - "127.0.0.1:55435:5432"
    volumes:
      - fitcore-postgres17-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  fitcore-postgres17-data:
